# 应用标题
VITE_APP_TITLE=Fast Vue3

# API 基础路径（后端接口）
# 默认 /api：开发期由 Vite 代理到 Nitro Mock 服务（端口 5320，随 dev 自动拉起）。
# 对接真实后端时改为后端地址，例如 http://localhost:5320
VITE_APP_API_BASEURL=/api

# Mock 开关（true/false）
VITE_MOCK_ENABLE=true
